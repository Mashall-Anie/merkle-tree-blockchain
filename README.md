# 🔗 Merkle Tree Blockchain

Bài tập lớn - Ứng dụng minh họa Merkle Tree trong Blockchain.

---

## 📦 Cài đặt và Chạy

### 0. Di chuyển vào thư mục project
```bash
cd merkle-tree-blockchain
```

### 1. Tạo môi trường ảo
```bash
python -m venv venv
```

### 2. Kích hoạt môi trường ảo

**Linux/macOS:**
```bash
# Fish shell:
source venv/bin/activate.fish

# Bash/Zsh:
source venv/bin/activate
```

**Windows:**
```bash
# Command Prompt (CMD):
venv\Scripts\activate.bat

# PowerShell:
venv\Scripts\Activate.ps1

# Git Bash:
source venv/Scripts/activate
```

**Lưu ý Windows PowerShell:** Nếu gặp lỗi "cannot be loaded because running scripts is disabled", chạy lệnh:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 3. Cài đặt thư viện
```bash
pip install -r requirements.txt
```

### 4. Chạy ứng dụng
```bash
python app.py
```

### 5. Thoát môi trường ảo
```bash
deactivate
```

---

## 🌐 Truy cập

Sau khi chạy, mở trình duyệt:
```
http://localhost:5000
```

---

